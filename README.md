# Ревизор

MCP-сервер и агент, который проверяет сайт по чек-листу и собирает отчёт.

Проект собирается по этапам. Сейчас готов **этап 1**: один рабочий инструмент
и подтверждённая связка с моделью.

## Что уже умеет

| Инструмент | Что делает |
|---|---|
| `check_url` | Код ответа, полная цепочка редиректов, ключевые заголовки, время ответа |

Проверки, которые уже даёт `check_url`: лишние шаги в цепочке редиректов, петли,
битые адреса, отсутствие `Last-Modified`, раскрытие версий в заголовках `Server`
и `X-Powered-By`, отсутствие `Strict-Transport-Security`, ответ без сжатия.

## Устройство

```
src/checks/url.js   логика проверки — обычная функция, ничего не знает про MCP
src/server.js       оболочка MCP: объявляет инструменты и принимает вызовы
test/check.js       запуск проверки напрямую, без модели
test/mcp.js         разговор с сервером по протоколу вручную, без Claude Code
.mcp.json           подключение сервера к Claude Code
```

Логика намеренно отделена от протокола. Если функция неправильно работает
в `test/check.js`, через MCP она правильнее не станет — отлаживать нужно там.

## Запуск

Проверить один адрес:

```bash
npm run check https://example.com/
```

Убедиться, что сервер отвечает по протоколу:

```bash
npm run mcp
```

## Подключение к Claude Code

Файл `.mcp.json` уже в репозитории. Откройте папку проекта в Claude Code
и перезапустите его — сервер подхватится автоматически, инструмент `check_url`
появится в списке доступных.

Если сервер не запустился, замените в `.mcp.json` относительный путь на полный:

```json
{ "mcpServers": { "seo-revizor": { "command": "node", "args": ["D:/QQQAAA/seo-revizor/src/server.js"] } } }
```

## Важно при доработке

Общение с клиентом идёт через стандартный ввод-вывод. `console.log` пишет
в тот же поток и ломает протокол — для отладки используйте только `console.error`.

## Дальше

- Этап 2: остальные инструменты (мета-теги, robots и карта сайта, ссылки, изображения, заголовки безопасности, скорость)
- Этап 3: чек-листы отдельными файлами данных
- Этап 4: агент, который сам решает, что вызывать
- Этап 5: отчёт и сравнение с предыдущим прогоном
